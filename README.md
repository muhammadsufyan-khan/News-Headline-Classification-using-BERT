# News-Headline-Classification-using-BERT
The goal of this task is to **classify news headlines** into predefined categories using **state-of-the-art BERT transformer models**.   The task demonstrates how **pre-trained language models** can be fine-tuned on domain-specific data to achieve high accuracy and F1-score for text classification tasks.
